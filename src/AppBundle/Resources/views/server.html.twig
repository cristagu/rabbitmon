<div class="card card-outline-primary ">
    <div class="card-header">
        <div class="float-right">
            <a class="btn btn-sm btn-outline-danger" href="{{ path('server_delete', {'name': server.name}) }}"><i class="fa fa-times"></i></a>
            <a class="btn btn-sm btn-outline-primary" href="{{ path('new_queue', {'name': server.name}) }}"><i class="fa fa-plus"></i></a>
        </div>
        <h5>{{ server.name }}</h5>
    </div>
    <div class="card-block">
        {#<table class="table">#}
            {#<tbody>#}
            {#{% for queue in server.queues %}#}
                {#{% if queue.messages > 10000 %}#}
                    {#<tr class="table-danger">#}
                {#{% elseif queue.messages > 1000 %}#}
                    {#<tr class="table-warning">#}
                {#{% else %}#}
                    {#<tr>#}
                {#{% endif %}#}
                    {#<td>{{ queue.vhost }}</td>#}
                    {#<td>{{ queue.name }}</td>#}
                    {#<td>{{ queue.messages }}</td>#}
                    {#<td>#}
                        {#<a class="btn btn-sm btn-outline-danger" href="{{ path('queue_delete', {'id': queue.id}) }}"><i class="fa fa-times"></i></a>#}
                    {#</td>#}
                {#</tr>#}
            {#{% endfor %}#}
            {#</tbody>#}
        {#</table>#}
            {% for queue in server.queues %}
                {% if queue.messages > 10 %}
                    <div class="queue-container bg-danger text-white">
                {% elseif queue.messages > 1 %}
                    <div class="queue-container bg-warning text-white">
                {% else %}
                    <div class="queue-container">
                {% endif %}
                    <div class="row">
                        <div class="col-2">{{ queue.vhost }}</div>
                        <div class="col-8">{{ queue.name }}</div>
                        <div class="col-1">{{ queue.messages }}</div>
                    </div>
                    <div class="delete">
                        <a href="{{ path('queue_delete', {'id': queue.id}) }}"> DELETE <i class="fa fa-times"></i></a>
                    </div>
                </div>
            {% endfor %}
    </div>
</div>